name: Notify the Agentic Law Review Editor

on:
  schedule:
    - cron: "7,22,37,52 * * * *"
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: notify-agentic-law-review-editor
  cancel-in-progress: false

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check mail configuration
        id: mail
        env:
          SMTP_USERNAME: ${{ secrets.ALR_SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.ALR_SMTP_PASSWORD }}
        run: |
          if [ -n "$SMTP_USERNAME" ] && [ -n "$SMTP_PASSWORD" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "enabled=false" >> "$GITHUB_OUTPUT"
            echo "Set ALR_SMTP_USERNAME and ALR_SMTP_PASSWORD to enable email notifications."
          fi

      # The cache contains manuscript numbers only. It prevents repeated mail
      # without publishing pending-submission metadata in the repository.
      - name: Restore notification state
        if: steps.mail.outputs.enabled == 'true'
        uses: actions/cache/restore@v4
        with:
          path: .notification-state
          key: alr-notifications-bootstrap
          restore-keys: |
            alr-notifications-

      - name: Email new submissions
        id: notify
        if: steps.mail.outputs.enabled == 'true'
        env:
          API_BASE: https://niccoloridi.com
          AYIL_ADMIN_KEY: ${{ secrets.AYIL_ADMIN_KEY }}
          SMTP_HOST: ${{ vars.ALR_SMTP_HOST || 'smtp.gmail.com' }}
          SMTP_PORT: ${{ vars.ALR_SMTP_PORT || '587' }}
          SMTP_USERNAME: ${{ secrets.ALR_SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.ALR_SMTP_PASSWORD }}
          SMTP_FROM: ${{ secrets.ALR_SMTP_FROM }}
          ALR_NOTIFICATION_TO: niccolo.ridi@kcl.ac.uk
        run: python3 .github/scripts/notify-yearbook.py

      - name: Save notification state
        if: steps.notify.outputs.state_changed == 'true'
        uses: actions/cache/save@v4
        with:
          path: .notification-state
          key: alr-notifications-${{ steps.notify.outputs.state_hash }}
